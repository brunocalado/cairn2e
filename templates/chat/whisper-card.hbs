{{!-- A whisper, as its body: the message's `flavor` says who it went to and is the caption, the
      speaker is the character, and the card draws no frame of its own. `text` arrives escaped,
      with its line breaks as `<br>` — the only markup in it is the markup the sender could not
      type (`module/apps/whisper.js`). --}}
<p class="cairn-card-lead">{{{text}}}</p>
